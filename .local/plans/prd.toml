[[stories]]
category = "functional"
description = "As a developer, I don't want any lint or test issue in the codebase."
steps = [
  "Run the `cargo xtask lint` command an gather the output",
  "Fix the issues found on the output.",
  "Re-run the `cargo xtask lint` command to check if the warnings and issues were fixed, or if new ones appear.",
  "Repeat the process until all lint and tests pass",
]
passes = true
acceptance = [
  "The `cargo xtask lint` command passes without warnings, erros, or issues.",
]
